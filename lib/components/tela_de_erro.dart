import 'package:flutter/material.dart';

class TelaDeErro extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.orange,
        appBar: AppBar(), body: Center(child: Text("erro de rota")));
  }
}
